import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titulo: {
    color: "#000",
    fontWeight: 900,
    fontSize: 25,
    marginTop: 20,
    marginBottom: 20
  }
});

export default styles;